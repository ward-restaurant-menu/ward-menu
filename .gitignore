# 1. Ignore absolutely everything by default
*

# 2. Allow Git to look inside all subdirectories 
# (Without this, files inside folders will be ignored)
!*/

# 3. Whitelist specific file extensions
!*.pdf
!*.html

# 4. Whitelist the .gitignore file itself so changes can be tracked
!.gitignore
